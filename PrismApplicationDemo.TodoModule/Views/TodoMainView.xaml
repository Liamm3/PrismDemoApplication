<UserControl x:Class="PrismApplicationDemo.TodoModule.Views.TodoMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:shared="clr-namespace:PrismApplicationDemo.Shared;assembly=PrismApplicationDemo.Shared"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ContentControl prism:RegionManager.RegionName="{x:Static shared:RegionNames.TodoList}" />
        <ContentControl Grid.Column="1" prism:RegionManager.RegionName="{x:Static shared:RegionNames.TodoDetail}" />
    </Grid>
</UserControl>
